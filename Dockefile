# 1. Use the official Python 3.9 base image
FROM python:3.9-slim

# 2. Set the working directory in the container
WORKDIR /app

# 3. Copy the current directory contents into the container at /app
COPY . /app

# 4. Install the dependencies from requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# 5. Expose port 5070 for the Flask app
EXPOSE 5070

# 6. Define the command to run the Flask application
# Assuming your main app file is named 'app.py'
CMD ["python", "app.py"]
